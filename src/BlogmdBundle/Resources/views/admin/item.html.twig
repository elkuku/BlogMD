{% extends '@App/base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('bundles/app/css/prism.css') }}">
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/app/js/prism.js') }}"></script>
{% endblock %}

{% block body %}

    <a href="{{ path('blog-edit', {'id': post.id}) }}">Edit</a>
    <a href="{{ path('blog-delete', {'id': post.id}) }}">Delete</a>

    {{ post.content|md2html }}

{% endblock %}

